#!/bin/bash

# Docker 配置初始化示例配置文件
# 将此文件复制为 init-docker.config 或其他名称并根据您的环境修改值

# 项目目录路径（可选，如果指定会自动切换到该目录）
PROJECT_DIR="/path/to/your/project"

# 项目名称（用于镜像命名和容器命名）
PROJECT_NAME="my-app"

# 项目类型 (nextjs|react|vue|angular|express)
PROJECT_TYPE="nextjs"

# 应用端口
APP_PORT="3000"

# 构建命令（默认: npm run build）
BUILD_COMMAND="yarn build"

# 包管理器 (yarn|npm|pnpm) - 可选，留空则自动检测
PACKAGE_MANAGER="yarn"